eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  #注册中心地址
server:
  port: 8762  #发布服务的端口号
spring:
  application:
    name: service-provider1  #在注册中心注册的服务名称，应用根据此服务名称进行调用服务